{
  "name": "ecs-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitest && vite",
    "build": "vitest && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "happy-dom": "^6.0.4",
    "typescript": "^4.6.4",
    "vite": "^3.0.0",
    "vitest": "^0.21.0"
  },
  "dependencies": {
    "madge": "^5.0.1"
  }
}
